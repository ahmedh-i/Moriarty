from setuptools import setup, find_packages

setup(
    name = 'Moriarty',
    version = '1.0.0',
    url = '',
    description = '',
    packages = find_packages(),
    install_requires = [
        # Github Private Repository
    ]
)